version: '3'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    links:
      - mongo
 
  mongo:
    image: mongo:latest
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: rootpassword
    ports:
      - 27017:27017